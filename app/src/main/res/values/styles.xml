<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.Custom" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#f21919</item>
        <item name="colorPrimaryDark">#681010</item>
        <item name="colorAccent">#2e0000</item>
        <item name="android:typeface">monospace</item>
        <item name="buttonStyle">@style/ButtonCustom</item>
        <item name="actionMenuTextColor">#aa0000</item>
    </style>

    <style name="ButtonCustom" parent="Widget.AppCompat.Button">
        <item name="android:textColor">#0099ff</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">1dp</item>
        <item name="background">@color/colorPrimaryDark</item>
        <item name="android:minWidth">88dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:layout_margin">3dp</item>

    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
